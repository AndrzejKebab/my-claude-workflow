Memory is DISABLED for this setup. It is machine-local (this dir is in no git repo and is keyed to this box's project path), so a new machine = lost methodology — the failure mode that makes it worthless.

Do NOT add memory files here. Persist knowledge where `git clone` carries it instead: skills/agents in `~/_dev/my-claude-workflow`, the project's git-tracked `CLAUDE.md` or docs, or the global config (once it lives in the workflow repo). See `~/.claude/CLAUDE.md` → "## Persistence — skills and git-tracked docs, NEVER machine-local memory (binding)".
