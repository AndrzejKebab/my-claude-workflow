# Project Memory

## UPM Migration Notes

### package.json Requirements
- Always include `"hideInEditor": false` to ensure packages appear in Package Manager UI
- Use `"autoReferenced": false` in all .asmdef files for isolation
