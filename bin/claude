#!/usr/bin/env bash
set -euo pipefail

EDITOR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)/claude-editor
export EDITOR
export VISUAL="$EDITOR"
export CLAUDE_LAUNCH_CWD="$PWD"

exec "$HOME/.local/bin/claude" "$@"
